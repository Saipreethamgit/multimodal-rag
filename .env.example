# Database
DATABASE_URL=postgresql://user:password@localhost:5432/multimodal_rag

# OpenAI - used for embeddings (text-embedding-3-small) and vision (gpt-4o)
OPENAI_API_KEY=sk-...

# Upload directory (relative to project root)
UPLOAD_DIR=./uploads
